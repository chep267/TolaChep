/**
 *
 * @author dongntd@bkav.com on 06/09/2022.
 *
 */

import { CSSObject } from 'styled-components';
import { FONT_SIZE, LIGHT_HEIGHT } from '@module-theme/constants';

export const textSmall: CSSObject = {
    fontWeight: 400,
    fontSize: FONT_SIZE.small,
    lineHeight: LIGHT_HEIGHT.small,
};

export const textSmallBold: CSSObject = {
    fontWeight: 600,
    fontSize: FONT_SIZE.small,
    lineHeight: LIGHT_HEIGHT.small,
};

export const textBase: CSSObject = {
    fontWeight: 400,
    fontSize: FONT_SIZE.base,
    lineHeight: LIGHT_HEIGHT.base,
};

export const textBaseBold: CSSObject = {
    fontWeight: 600,
    fontSize: FONT_SIZE.base,
    lineHeight: LIGHT_HEIGHT.base,
};

export const textMedium: CSSObject = {
    fontWeight: 400,
    fontSize: FONT_SIZE.medium,
    lineHeight: LIGHT_HEIGHT.medium,
};

export const textMediumBold: CSSObject = {
    fontWeight: 600,
    fontSize: FONT_SIZE.medium,
    lineHeight: LIGHT_HEIGHT.medium,
};

export const textLarge: CSSObject = {
    fontWeight: 400,
    fontSize: FONT_SIZE.large,
    lineHeight: LIGHT_HEIGHT.large,
};

export const textLargeBold: CSSObject = {
    fontWeight: 600,
    fontSize: FONT_SIZE.large,
    lineHeight: LIGHT_HEIGHT.large,
};

export const textHuge: CSSObject = {
    fontWeight: 400,
    fontSize: FONT_SIZE.huge,
    lineHeight: LIGHT_HEIGHT.huge,
};

export const textHugeBold: CSSObject = {
    fontWeight: 600,
    fontSize: FONT_SIZE.huge,
    lineHeight: LIGHT_HEIGHT.huge,
};
